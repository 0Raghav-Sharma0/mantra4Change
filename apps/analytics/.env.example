# =============================================================================
# Mantra4Change Analytics — apps/analytics/.env
# Copy: cp apps/analytics/.env.example apps/analytics/.env
# =============================================================================

HOST=0.0.0.0
ANALYTICS_PORT=8000
LOG_LEVEL=info

DATA_ROOT=../../data
PBL_CSV_DIR=../../data/pbl/csv_exports
GRANT_CSV_DIR=../../data/grant/csv

CHART_OUTPUT_DIR=./output/charts
REPORTS_DIR=./reports

NODE_SERVER_URL=http://localhost:5000

# Legacy alias (optional; ANALYTICS_PORT takes precedence)
# PORT=8000
